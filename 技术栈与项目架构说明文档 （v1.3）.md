# 技术栈与项目架构说明文档 （v1\.3）

## 概述

本文档旨在详细记录本项目的技术选型、架构层次及其核心工具，确保开发团队在一致的环境和标准下协作。

## 架构概览

### 2\.1 前端展现层 （Frontend）

- **核心框架**：React 18

- **构建工具**：Vite

- **状态管理**：Zustand / Redux Toolkit

- **UI 组件库**：Tailwind CSS \+ Shadcn UI

- **网络请求**：Axios \+ TanStack Query

- **主要掌握，组件封装化设计，需求拆解，样式拆解**

### 2\.2 后端服务层 （Backend）

- **运行环境**：Node\.js （LTS） / Python 3\.10\+

- **开发框架**：Django

- **通信协议**：RESTful API

- **身份认证**：JWT （JSON Web Token）

- **了解代码解耦合\(入门\)**

- **了解基本锁**

- **了解基本设计模式**

### **2\.3 数据持久层 （Da**tabase \& Storage）

- **关系型数据库**：MySQL / PostgreSQL

- **缓存系统**：Redis（Session 管理与高频缓存）

- **对象存储**：阿里云 OSS

### 2\.4 基础设施与运维 （DevOps）

- **反向代理**：Nginx

- **容器化**：Docker

- **CI/CD**: GitHub Actions / GitLab CI

---

## 基础文件结构 （Project Structure）

### 3\.1 前端目录结构

```Plain Text
frontend/
├── public/                 # 静态资源
├── src/
│   ├── assets/             # 媒体资源（图片、字体等）
│   ├── components/         # 公共 UI 组件
│   ├── hooks/              # 自定义 React Hooks
│   ├── layouts/            # 页面布局模板
│   ├── pages/              # 业务路由页面
│   ├── services/           # API 请求封装
│   ├── store/              # 状态管理
│   ├── utils/              # 工具函数
│   └── App.tsx             # 路由配置入口
├── .env.development        # 环境变量
└── vite.config.ts          # Vite 配置文件
```

### 3\.2 后端目录结构

```Plain Text
backend/
├── src/
│   ├── config/          # 全局配置
│   ├── controllers/     # 接口路由层
│   ├── services/        # 业务逻辑层
│   ├── models/          # 数据库模型定义
│   ├── middlewares/     # 中间件 (权限、日志)
│   ├── main.ts          # 服务启动入口
│   └── app.ts           # Express 应用实例
├── .env                 # 敏感环境变量
└── docker-compose.yml   # 容器化编排
```

---

## 协作规范

### 4\.1 命名规范

- **文件夹**：小写中划线 `kebab-case`（如 `user-profile/`）。

- **组件/类名**：大驼峰 `PascalCase`（如 `UserCard.tsx`）。

- **变量/函数**：小腿峰 `camelCase`。

### 4\.2 Git 提交规范

遵循 `type: description` 格式：

- `feat`： 新功能

- `fix`： 修补 Bug

- `docs`： 文档变更

- `style`： 代码格式修改（不影响逻辑）

---

## 环境要求

- **Node\.js**: \>= 18\.x

- **Docker**: \>= 20\.x

- **IDE**: 推荐使用 VS Code

---

### 完整项目文件结构 （整合版）

```Plain Text
.
├── CLAUDE.md                # 项目核心便签 (队规，进 Git)
├── CLAUDE.local.md          # 个人小抄 (本地环境/偏好，不进 Git)
├── .claude/                 # Claude 配置文件夹
│   └── rules/               # 分片规则集 (超 200 行时拆分)
│       ├── ui-style.md      # 针对 frontend/src/components 的 UI 规范
│       └── api-conventions.md # 针对前后端接口对接的规范
├── frontend/                # --- 原始前端结构 ---
│   ├── public/              # 静态资源
│   ├── src/
│   │   ├── assets/          # 媒体资源
│   │   ├── components/      # 公共 UI 组件
│   │   ├── hooks/           # 自定义 React Hooks
│   │   ├── layouts/         # 页面布局模板
│   │   ├── pages/           # 业务路由页面
│   │   ├── services/        # API 请求封装
│   │   ├── store/           # 状态管理
│   │   ├── utils/           # 工具函数
│   │   └── App.tsx          # 路由配置入口
│   ├── .env.development     # 环境变量
│   └── vite.config.ts       # Vite 配置文件
├── backend/                 # --- 原始后端结构 ---
│   ├── src/
│   │   ├── config/          # 全局配置
│   │   ├── controllers/     # 接口路由层
│   │   ├── services/        # 业务逻辑层
│   │   ├── models/          # 数据库模型定义
│   │   ├── middlewares/     # 中间件 (权限、日志)
│   │   ├── main.ts          # 服务启动入口
│   │   └── app.ts           # Express 应用实例
│   ├── .env                 # 敏感环境变量
│   └── docker-compose.yml   # 容器化编排
├── .gitignore
└── README.md
```

---

### 📝 核心规范文件模板

#### 1\. `CLAUDE.md` （根目录）

```Plain Text
# 项目核心开发规范 @CLAUDE.md## 1. 技术栈确认- Frontend: React + Vite + TypeScript (见 ./frontend)
- Backend: TypeScript/Node.js (见 ./backend)

## 2. 核心指令 - 前端开发: `cd frontend && pnpm dev`- 后端开发: `cd backend && npm run dev`- 全栈构建: `docker-compose up --build`## 3. 代码约定- 统一使用 2 空格缩进，无分号。
- 前端组件必须放在 `frontend/src/components` 且使用大驼峰命名。
- 所有 API 请求必须在 `frontend/src/services` 中封装，禁止在页面直接写 fetch/axios。
- 详细 UI 规范参考 @.claude/rules/ui-style.md

## 3. 严禁行为 (Critical Prohibitions)

- **严禁提交依赖**: 绝对禁止运行 `git add node_modules/`。如果不小心追踪了，请立即运行 `git rm -r --cached node_modules/`。
- **敏感信息屏蔽**: 禁止将任何 `.env` 文件或包含 API Key 的硬编码字符串提交到 Git。所有配置必须通过环境变量读取。
- **数据库隔离**: 禁止提交任何本地数据库文件 (`.db`, `.sqlite`)。数据库变更必须通过 `backend/src/models/` 中的模型定义来同步。
- **提交前自检**: Claude 在协助生成 Git Commit 建议前，请务必检查是否有上述文件出现在暂存区。
```

#### 2\. `.claude/rules/ui-style.md` （针对性分片）

```Plain Text
# UI 开发规则# paths: ["frontend/src/components/****", "frontend/src/pages/****"]- 必须保证组件是响应式的。
- 所有的圆角必须统一使用 `border-radius: 8px` 或以上。
- 逻辑 Hooks 必须提取到 `frontend/src/hooks` 中。
```

#### 3\. `CLAUDE.local.md` （本地专属）

```Plain Text
# 本地私密便签 (不进 Git)- 回答语言: 始终使用中文。
- 调试偏好: 每次修复 bug 后，请自动尝试运行测试脚本。
- 本地端口: 前端 5173, 后端 3000。
```

### 💡 维护要点：

1. **Say It Once**: 只要写在 `CLAUDE.md` 里，Claude 每次启动都会读取，你不需要在对话里重复强调目录结构。

2. **200 行红线**： 如果 `CLAUDE.md` 变得太长，请将具体的业务逻辑（如“如何写 Controller”）移动到 `.claude/rules/` 下。

3. **保持同步**： 当调整了目录结构（比如新增了 `tests/`），记得同步更新 `CLAUDE.md`。

---

### `.gitignore` 物理屏蔽

在你的项目根目录下，确保 `.gitignore` 包含以下内容，彻底封死这些违规文件的上传路径：

```Plain Text
# --- 依赖与构建 ---
node_modules/
dist/
build/
.pnpm-debug.log*

# --- 敏感环境信息 (绝对禁止上传) ---
.env
.env.local
.env.*.local
CLAUDE.md
.claude/
*.local.md
frontend/.env.development
backend/.env
SOUL.md
STRUCTURE.md
USER.md

# --- 数据库与日志 ---
*.db
*.sqlite
*.log
backend/data/
.data/

# --- 编辑器与系统 ---
.DS_Store
.vscode/
.idea/
```

---

## 参考目录文件

\[虚树互动规范\.zip\]



